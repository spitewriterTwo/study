绑定用户(打开git-bash.exe)
git config --global user.name "spitewriter"
git config --global user.email "1379864702@qq.com"
查看ssh
cd ~/.ssh ls
生成ssh
ssh-keygen -t rsa -C "1757846498@qq.com"
建立本地仓库(进入带上传的文件所在的文件夹，init初始化)
cd g:\text
git init
添加文件到本地仓库 
git add .  加上  git commit -m "First initial commit"
关联GitHub的仓库
git remote add origin https://github.com/spitewriter/text.git
上传本地代码
git push -u origin master
